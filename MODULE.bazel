module(
    name = "d2xx_test",
    version = "0.1.0",
)

# C++ build rules (cc_library, cc_binary, cc_import, etc.)
bazel_dep(name = "rules_cc", version = "0.2.17")

# Platform constraints (@platforms//os:windows, @platforms//os:linux, etc.)
bazel_dep(name = "platforms", version = "1.0.0")
